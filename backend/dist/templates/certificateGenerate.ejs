<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Certificate of Completion</title>
    <!-- Tambahkan di dalam <head> -->
    <link
      href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap"
      rel="stylesheet"
    />

    <style>
      html,
      body {
        margin: 0;
        padding: 0;
        width: 1123px;
        height: 794px;
        background-color: #ffffff;
        font-family: "Segoe UI", sans-serif;
      }

      .certificate {
        width: 100%;
        height: 100%;
        background-image: url("https://res.cloudinary.com/dyjsmoxmq/image/upload/v1749892512/djwjfx9ixso8ubedckfm.jpg");
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
        padding: 80px 100px 60px 300px;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        color: #111827;
      }

      h1 {
        font-family: "Great Vibes", cursive;
        font-size: 48px;
        color: #047857;
        margin: 0 0 10px;
      }

      h2 {
        font-size: 20px;
        font-weight: 400;
        margin: 0;
        color: #374151;
      }

      .user-name {
        font-size: 32px;
        font-weight: bold;
        margin: 20px 0;
      }

      .info {
        font-size: 18px;
        color: #1f2937;
        margin-bottom: 20px;
      }

      .footer {
        font-size: 14px;
        color: #6b7280;
      }

      .bottom-section {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
      }

      .qr-box,
      .signature-box {
        text-align: center;
      }

      .qr-box img {
        width: 100px;
        height: 100px;
      }

      .qr-text {
        font-size: 12px;
        color: #9ca3af;
        margin-top: 8px;
      }

      .signature-box img {
        width: 150px;
        height: auto;
      }

      .sign-name {
        font-weight: bold;
        font-size: 14px;
        margin: 5px 0 0;
      }

      .sign-title {
        font-size: 12px;
        color: #6b7280;
        margin: 0;
      }
    </style>
  </head>
  <body>
    <div class="certificate">
      <div>
        <h1>Certificate of Completion</h1>
        <h2>This certifies that</h2>
        <div class="user-name"><%= userName %></div>

        <div class="info">
          has successfully completed the assessment<br />
          <strong><%= assessmentTitle %></strong><br />
          with a score of <strong><%= score %></strong> out of
          <strong><%= totalPoints %></strong>
        </div>
        <div class="footer">
          This certificate is presented by <strong>JobsDoors</strong><br />
          Issued on: <%= new Date(completedAt).toLocaleDateString("en-US", {
          year: "numeric", month: "long", day: "numeric", }) %>
        </div>
      </div>

      <div class="bottom-section">
        <div class="qr-box">
          <img
            src="https://api.qrserver.com/v1/create-qr-code/?size=100x100&data=<%= domain %>/verify-certificate/<%= certificateId %>"
            alt="QR Code"
          />
          <div class="qr-text">Scan to verify authenticity</div>
        </div>

        <div class="signature-box">
          <img
            src="https://res.cloudinary.com/dyjsmoxmq/image/upload/v1749892941/k5biuruolwhgghsvz4g5.png"
            alt="CEO Signature"
          />
          <p class="sign-name">John Doe</p>
          <p class="sign-title">Chief Executive Officer</p>
        </div>
      </div>
    </div>
  </body>
</html>
